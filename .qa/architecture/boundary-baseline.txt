# Finite baseline of known slice-boundary violations (Vertical-Slice Strangler).
# Format: <kind><TAB><path><TAB><import-specifier>
# kind: deep = cross-product-slice deep import; reverse = platform/facade → module interna
# New violations NOT listed here fail scripts/checks/boundary-imports.sh.
# Remove a line only when the corresponding import is fixed (cleanup issues).
# Public slice entry (modules/<slice> or index) is allowed for facades/adapters.
